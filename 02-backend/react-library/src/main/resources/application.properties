# Database connection properties
spring.datasource.url=jdbc:sqlserver://localhost:1433;databaseName=reactLibrary;Encrypt=true;TrustServerCertificate=true
spring.datasource.username=nhat1
spring.datasource.password=1234

# Specify the SQL Server dialect
spring.jpa.database-platform=org.hibernate.dialect.SQLServer2012Dialect

# (Optional) Set Hibernate to create/update the database schema
spring.jpa.hibernate.ddl-auto=none

spring.data.rest.base-path=/api

okta.oauth2.client-id=0oacorkbhcNpM15GA5d7
okta.oauth2.issuer=https://dev-59451238.okta.com/oauth2/default

#####
#
# HTTPS configuration
#
#####
# Server web port
server.port=8443
# Enable HTTPS support (only accept HTTPS requests)
server.ssl.enabled=true
# Alias that identifies the key in the key store
server.ssl.key-alias=luv2code
# Keystore location
server.ssl.key-store=classpath:luv2code-keystore.p12
# Keystore password
server.ssl.key-store-password=secret
# Keystore format
server.ssl.key-store-type=PKCS12